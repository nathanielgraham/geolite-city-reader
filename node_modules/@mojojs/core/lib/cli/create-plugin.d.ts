import type { App } from '../app.js';
/**
 * Plugin generator command.
 */
declare function genPluginCommand(app: App, args: string[]): Promise<void>;
declare namespace genPluginCommand {
    var description: string;
    var usage: string;
}
export default genPluginCommand;
